import styles from "./Supervisor.module.scss";
import { SupervisorProps } from "./Supervisor.types.ts";

const Supervisor = ({}: SupervisorProps) => {
  return <div></div>;
};

export default Supervisor;
